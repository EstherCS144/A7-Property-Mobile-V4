import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.a7property.app",
  appName: "A7 Property",
  webDir: "out",
  android: {
    backgroundColor: "#F9F7FF",
  },
};

export default config;
